interface:
  display_name: "Proxying Dev Server"
  short_description: "Dev server reverse proxy on NixOS MiniPC via dev-proxy and Caddy HTTPS. Triggers: \"dev-proxy 설정\", \"dev server 프록시\", \"HMR이 안 됨\", "
  default_prompt: "Use $proxying-dev-server to help with this task in nixos-config."
