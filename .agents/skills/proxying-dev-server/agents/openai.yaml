interface:
  display_name: "Proxying Dev Server"
  short_description: "This skill should be used when setting up or troubleshooting the dev server reverse proxy on NixOS MiniPC. It covers the dev-pro"
  default_prompt: "Use $proxying-dev-server to help with this task in nixos-config."
