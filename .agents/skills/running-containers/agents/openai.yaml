interface:
  display_name: "Running Containers"
  short_description: "Use this skill when the user asks about Podman/Docker containers, homeserver services (immich, uptime-kuma, copyparty, vaultward"
  default_prompt: "Use $running-containers to help with this task in nixos-config."
