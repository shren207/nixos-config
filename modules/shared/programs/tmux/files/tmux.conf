# [2025-10-20]

# tmux 설정 관련 주의사항
# - 경로 설정은 ~/ 이런 식으로 하면 안되고, $HOME을 사용해야 함
# - pane_title은 계속 자동으로 바뀌는데, 이를 막을 수는 없음
# - source-file(config reload)만으로는 기존 키맵이 자동으로 해제되지 않음 (= unbind-key를 직접 입력해 주어야 함)

# Claude Code가 pane 이름을 자동으로 변경하지 못하도록 막는 설정 (❌ 이 설정이 존재해도 pane_title이 자동으로 변경되는 것을 막을 수 없었기에, custom_pane_title을 사용하는 방식으로 변경되었음)
set -g automatic-rename off
set -g allow-rename off # 이거 하나만으로는 해결되지 않음

# === 터미널 호환성 설정 ===
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

# mouse로 스크롤 가능하게 하는 기능 (tmux는 기본적으로 방향키로 스크롤이 안되는데, 이를 위해서는 copy-mode를 사용해야 하는데 여간 불편한 것이 아니다. 이렇게 마우스로 스크롤을 활성화해야 삶의 질이 올라가므로, 해당 기능을 활성화하는 것은 필수이다)
set -g mouse on

# === 스크롤 개선 설정 ===
# 스크롤 기록 행 수를 늘리기
set -g history-limit 50000

# === 마우스 휠 스크롤 자동화 설정 ===
# 마우스 휠을 올릴 때:
# 1. 마우스 이벤트를 사용하는 앱(vim, less 등)이 실행 중이면 해당 앱에 이벤트 전달
# 2. 이미 copy-mode라면 한 줄 위로 스크롤
# 3. 그 외의 경우 copy-mode로 진입하며 한 줄 위로 스크롤
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -X scroll-up' 'copy-mode -e; send-keys -X scroll-up'"

# 마우스 휠을 내릴 때:
# copy-mode 상태에서 맨 아래까지 내려가면 자동으로 copy-mode 종료
bind -n WheelDownPane select-pane -t= \; send-keys -M

# --- 재바인딩 충돌 방지 ---
unbind -T prefix n
unbind -T prefix y
unbind -T prefix v
unbind -T prefix u
unbind -T prefix P
unbind -T prefix r
unbind -T prefix N
unbind -T prefix K
unbind -T prefix a
unbind -T prefix V

# --- Help: prefix + a ---
bind-key -T prefix a run-shell "$HOME/.tmux/scripts/prefix-help.sh"

# --- prefix + N : 새 노트 생성(제목 입력 후 생성/열기) ---
bind-key -T prefix N command-prompt -p "New note title:" \
  "run-shell \"$HOME/.tmux/scripts/pane-note.sh new '%%'\""

# --- prefix + K : 기존 노트를 현재 pane에 연결 ---
bind-key -T prefix K run-shell "$HOME/.tmux/scripts/pane-link.sh"

# --- prefix + V : 기존 노트 미리보기 (연결 없이 read-only로 확인) ---
bind-key -T prefix V run-shell "$HOME/.tmux/scripts/pane-peek.sh"

# 설정 재로드를 쉽게 하기 위해 (🚧 legacy)
# bind r source-file ~/.tmux.conf \; display-message "Config reloaded!"
# 🚨 짧은 답: 네. source-file(config reload)만으로는 기존 키맵이 자동으로 해제되지 않습니다. (= unbind-key를 직접 입력해 주어야 함)
# tmux는 “현재 서버 상태”에 새 설정을 추가/덮어쓰기만 하고, 파일에서 지워졌다고 해서 예전 바인딩이 자동으로 풀리진 않아요.
# 기억해 둘 점
# •	같은 테이블/같은 키에 bind-key를 다시 하면 → 그 한 건만 덮어쓰기됩니다.
# (예: -T prefix o → 같은 -T prefix o로 다시 바인드하면 OK)
# •	하지만 과거에 다른 테이블(예: -n(root), -T copy-mode-vi)로 묶었던 게 있으면,
# rebind만으로는 그 테이블에 남아 계속 동작합니다. 그래서 명시적 unbind가 필요해요.

# --- reload ---
bind r source-file "$HOME/.tmux/tmux.conf" \; display-message "Config reloaded!"

# ===== Pane Notepad =====
set -g @pane_note_sticky 0          # 1로 바꾸면 같은 리포+제목이면 같은 노트 공유
run-shell 'mkdir -p "$HOME/.tmux/pane-notes"'

# 포커스가 들어올 때 해당 pane의 노트 경로 변수를 준비 
# set-hook -g pane-focus-in 'run-shell "~/.tmux/scripts/pane-note.sh ensure-var"' (❌ 이거 때문에 prefix + 0 이 동작하지 않음)
set-hook -g pane-focus-in 'run-shell "$HOME/.tmux/scripts/pane-note.sh ensure-var"'
# 원인: run-shell "…" 안에 "~/.tmux/…" 처럼 틸드(~)를 따옴표로 감싸면 셸이 ~ 확장을 하지 않음 → 그대로 ~/.tmux/…라는 경로를 실행하려다 실패 → 127.
# 결론: .tmux.conf 안의 run-shell 'mkdir -p "$HOME/…"' 는 $HOME이라 확장되므로 문제없습니다. 규칙은 “따옴표 안에 ~ 쓰지 말고, 항상 $HOME을 쓰자” 입니다.

# 단축키
bind-key n    run-shell "$HOME/.tmux/scripts/pane-note.sh edit"
bind-key y    run-shell "$HOME/.tmux/scripts/pane-note.sh add-clipboard"
bind-key v    run-shell "$HOME/.tmux/scripts/pane-note.sh view"      # 읽기전용
bind-key u    run-shell "$HOME/.tmux/scripts/pane-note.sh open-url"  # URL 열기

set-environment -g EDITOR "vim"

# 단축키 (이렇게 하면 실행이 명령어를 못 찾음. 127 나옴)
# bind-key n    run-shell '~/.tmux/scripts/pane-note.sh edit'           # 노트 편집(팝업)
# bind-key C-n  run-shell '~/.tmux/scripts/pane-note.sh view'           # 읽기 전용 보기
# bind-key y    run-shell '~/.tmux/scripts/pane-note.sh add-clipboard'  # 클립보드 한 줄 추가
# bind-key o    run-shell '~/.tmux/scripts/pane-note.sh open-url'       # 노트 내 URL 열기(옵션:fzf)

# Pane 테두리에 노트 존재 표시(🗒️). 네 pane-border-format의 #[default] 직전에 아래 토막을 끼워 넣기:
#   #{?@pane_note_path, #[fg=blue]#( [ -s "#{@pane_note_path}" ] && echo " 🗒️"),}
# 예) 네가 쓰는 줄을 그대로 살리고, 맨 끝 #[default] 직전에 한 칸 띄우고 삽입하면 됨.

# === Pane 디자인 설정 ===
# Pane 경계선을 가능한 한 굵고 눈에 띄게 설정
set -g pane-border-style fg=cyan,bg=default
set -g pane-active-border-style fg=red,bg=default,bold
set -g pane-border-lines heavy

# Pane 제목 표시 설정 (현재 경로, 사용자 지정 pane 제목)
set -g pane-border-status top
# [ 브랜치]: 사용자 제목  ─ Git 프로젝트가 아니면 🚨
set -g pane-border-format ' #{?pane_active,#[fg=yellow]#[bold],#[fg=cyan]#[bold]}#[fg=yellow]#[bold][#(cd "#{pane_current_path}" && branch=$(git symbolic-ref --short HEAD 2>/dev/null) && echo " $branch" || echo "🚨Git 프로젝트가 아님!")]#{?@custom_pane_title,: #[fg=magenta]#[bold]#{@custom_pane_title},: #[fg=magenta]#[dim]🚨pane 이름을 작성해주세요}#[default] #{?@pane_note_path, #[fg=blue]#( [ -s "#{@pane_note_path}" ] && echo " 🗒️"),}'

# (prev): [ 브랜치] [🗒️ 사용자 제목] ─ Git 프로젝트가 아니면 🚨
# set -g pane-border-format ' #{?pane_active,#[fg=yellow]#[bold],#[fg=cyan]#[bold]}#[fg=yellow]#[bold][#(cd "#{pane_current_path}" && branch=$(git symbolic-ref --short HEAD 2>/dev/null) && echo " $branch" || echo "🚨Git 프로젝트가 아님!")]#{?@custom_pane_title, #[fg=magenta]#[bold][🗒️ #{@custom_pane_title}], #[fg=magenta]#[dim][🚨pane 이름을 작성해주세요]}#[default]'

# (prev): [ 브랜치] [사용자 지정 제목] ─ Git 프로젝트가 아니면 🚨 
# (❌ 이건 동작하지 않음. pane-border-format 안에서 printf " %s" 처럼 % 가 그대로 들어가면, tmux가 이것을 strftime 포맷(예: %s → 유닉스 타임스탬프)으로 해석해 버립니다. 그래서 브랜치명이 아니라 1752483221 같은 숫자가 표시된 것입니다.
# set -g pane-border-format ' #{?pane_active,#[fg=yellow]#[bold],#[fg=cyan]#[bold]}#[fg=yellow]#[bold][#(cd "#{pane_current_path}" && branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null) && printf " %s" "$branch" || echo "🚨Git 프로젝트가 아님!")]#{?@custom_pane_title, #[fg=magenta]#[bold][#{@custom_pane_title}], #[fg=magenta]#[dim][🚨pane 이름을 작성해주세요]}#[default]'

# (prev): [브랜치] [사용자 지정 제목]  ── Git 프로젝트가 아니면 🚨 메시지
# set -g pane-border-format ' #{?pane_active,#[fg=yellow]#[bold],#[fg=cyan]#[bold]}#[fg=yellow]#[bold][#(cd "#{pane_current_path}" && git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "🚨Git 프로젝트가 아님!")]#{?@custom_pane_title, #[fg=magenta]#[bold][#{@custom_pane_title}], #[fg=magenta]#[dim][🚨pane 이름을 작성해주세요]}#[default]'

# (prev-0): 오직 custom_pane_title만 표시 (custom_pane_title가 없는 경우 '🚨pane 이름을 작성해주세요'로 표시)
# set -g pane-border-format ' #{?pane_active,#[fg=yellow]#[bold],#[fg=cyan]#[bold]}#{?@custom_pane_title, #[fg=magenta]#[bold][#{@custom_pane_title}], #[fg=magenta]#[dim][🚨pane 이름을 작성해주세요]}#[default]'

# (prev-1): 경로 + custom_pane_title (custom_pane_title가 없는 경우 '🚨pane 이름을 작성해주세요'로 표시)
# set -g pane-border-format ' #{?pane_active,#[fg=yellow]#[bold],#[fg=cyan]#[bold]}#(echo "#{pane_current_path}" | sed "s|^$HOME|~|")#{?@custom_pane_title, #[fg=magenta]#[bold][#{@custom_pane_title}], #[fg=magenta]#[dim][🚨pane 이름을 작성해주세요]}#[default]'

# (prev-2): 경로 + custom_pane_title (custom_pane_title가 없는 경우 fallback 없음)
# set -g pane-border-format \
#   ' #{?pane_active,#[fg=yellow]#[bold],#[fg=cyan]#[bold]}#(echo "#{pane_current_path}" | sed "s|^$HOME|~|")#{?@custom_pane_title, #[fg=magenta]#[bold][#{@custom_pane_title}],}#[default]'

# (prev-3): 경로 + branch + pane_title (pane_title이 수시로 변경되므로 위와 같이 custom_pane_title을 사용하는 것으로 변경하였음)
# set -g pane-border-format \
#   ' #{?pane_active,#[fg=yellow]#[bold],#[fg=cyan]#[bold]}#(echo "#{pane_current_path}" | sed "s|^$HOME|~|") #[fg=yellow]#[bold][#(cd "#{pane_current_path}" && git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "🚨Not a Git Project")]#{?pane_title, #[fg=magenta]#[bold][#{pane_title}],}#[default]'

# Pane 페인을 더욱 강조
set -g window-active-style 'fg=terminal,bg=terminal'
set -g window-style 'fg=colour247,bg=colour236'

# === 상태 표시줄 설정 (수정됨) ===
# 상태 표시줄을 화면 상단에 배치
set -g status-position top

# 상태 표시줄 기본 스타일(검은 배경에 흰색 문자)
set -g status-style bg=black,fg=white

# 창 목록을 왼쪽 정렬로 설정
set -g status-justify left

# 【왼쪽】세션 이름 표시
set -g status-left-length 20
set -g status-left '#[fg=cyan,bold]S: #S'

# 【오른쪽】현재 경로와 Git 브랜치를 표시
set -g status-right-length 150
# set -g status-right "#[fg=cyan,bold]#(echo '#{pane_current_path}' | sed 's|^/Users/glen|~|') #[fg=yellow,bold](#(cd '#{pane_current_path}' && git rev-parse --abbrev-ref HEAD 2>/dev/null || echo '')) #[default]"
# --- status-right: 홈 경로 치환을 $HOME 기준으로 ---
set -g status-right '#[fg=cyan,bold]#(sh -lc "printf %s \"#{pane_current_path}\" | sed \"s|^$HOME|~|\") #[fg=yellow,bold](#(cd "#{pane_current_path}" && git symbolic-ref --short HEAD 2>/dev/null || echo "")) #[default]'


# 【중앙 창 목록】의 스타일
# 비활성 창 (번호:이름)
setw -g window-status-format ' #I:#W '

# 현재 활성 창 (노란 배경으로 강조)
setw -g window-status-current-format '#[fg=black,bg=yellow,bold] #I:#W '

# 상태 표시줄 갱신 간격 (🚧 legacy: 1초마다 갱신되는데, 이건 너무 빠름)
# set -g status-interval 1

# --- 상태 표시줄 갱신 간격 (갱신 간격 완화 버전: 5초마다 갱신됨) ---
set -g status-interval 5

# === 기타 설정 ===
# 창 이름 자동 변경 비활성화 (수동으로 설정한 이름 유지)
set -g automatic-rename off

# 메시지 표시 시간을 길게 설정
set -g display-time 2000

# 세션 및 창 전환과 이름 변경을 쉽게 하기
bind-key s choose-session -F "#{?session_attached,#[fg=red bold],}#{session_name}: #{session_windows} windows"
bind-key , command-prompt -p "New window name:" "rename-window '%%'"
bind-key $ command-prompt -p "New session name:" "rename-session '%%'"
# 현재 pane에 사용자 지정 제목(@custom_pane_title)을 설정
bind-key P command-prompt -p "Custom pane title:" "set -p @custom_pane_title '%%'; display-message 'Pane title set to %%'"

# (prev): tmux의 pane_title 값을 변경하는 단축키였음
# bind-key P command-prompt -p "Pane name: " "select-pane -T '%%'"