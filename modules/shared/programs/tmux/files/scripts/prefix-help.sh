#!/usr/bin/env bash
set -euo pipefail

# 현재 pane 컨텍스트 정보(옵션값이 없으면 빈 문자열)
TITLE="$(tmux display-message -p '#{@custom_pane_title}')"
NOTE_PATH_RAW="$(tmux display-message -p '#{@pane_note_path}')"
NOTE_PATH="$NOTE_PATH_RAW"
[ -n "${NOTE_PATH:-}" ] && NOTE_PATH="$(printf "%s" "$NOTE_PATH" | sed "s|^$HOME|~|")"

# 보기용 텍스트 생성
TMP="$(mktemp -t tmux-help.XXXXXX)"
trap 'rm -f "$TMP"' EXIT

cat >"$TMP" <<EOF
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃  Custom tmux Prefix Cheatsheet (prefix + …)                  ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
현재 Pane 제목: ${TITLE:-<없음>}
현재 연결된 노트: ${NOTE_PATH:-<없음>}

[노트 관련]
  N : 새 노트 생성(제목 입력) → 생성 후 팝업으로 열기
  K : 기존 노트 연결(선택) → 현재 pane에 링크만 설정
  V : 기존 노트 열기(선택) → 연결 없이 cursor 편집기로 열기
  n : 노트 편집(팝업) — 링크 없으면 안내
  v : 노트 읽기(읽기 전용 팝업)
  y : 클립보드 한 줄을 노트에 추가
  u : 노트 내 URL 메뉴 열기(라벨/도메인별)

[이름·세션 관리]
  P : 현재 pane 사용자 지정 제목(@custom_pane_title) 설정
  , : 현재 창 이름 변경
  $ : 현재 세션 이름 변경
  s : 세션 선택 메뉴

[설정]
  r : tmux 설정 리로드 (source-file "\$HOME/.tmux/tmux.conf")

Tip
- prefix + :  → tmux 명령 프롬프트
- 현재 pane의 노트 경로 확인:  tmux display-message -p '#{@pane_note_path}'
- 노트 아이콘(🗒️)은 링크된 경로가 존재하고 파일 크기가 0보다 클 때 표시됨
EOF

# 팝업으로 보기: bat이 있으면 bat, 없으면 less
if command -v bat >/dev/null 2>&1; then
  tmux display-popup -E -w 85% -h 75% "bat -pp --paging=always '$TMP'"
else
  tmux display-popup -E -w 85% -h 75% "LESS= less -+F -+X -R '$TMP'"
fi