# nix build result
/result

# direnv cache (nix-direnv가 devShell 평가 결과 캐싱)
.direnv/

# Nix로 생성되는 Claude Code 스킬 (symlink → /nix/store/)
.claude/skills/viewing-immich-photo/

# Codex CLI (Nix activation으로 생성되는 심볼릭 링크/파생 파일)
# AGENTS.override.md는 Git 추적 대상 (Codex 전용 보충 규칙)
AGENTS.md
.agents/
.codex/
